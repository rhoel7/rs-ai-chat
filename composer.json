{
    "name": "reunify-studios/reunify-ai-chat",
    "description": "Multi-provider AI chat widget for WordPress/WooCommerce — swaps between Groq, Mistral, Gemini, OpenAI, Claude, DeepSeek, and Azure OpenAI, with persistent encrypted history, rate limiting, and a security-first architecture.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-openssl": "*",
        "ext-pdo": "*",
        "ext-pdo_mysql": "*",
        "ext-curl": "*",
        "ext-json": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "classmap": [
            "api/"
        ]
    },
    "scripts": {
        "test": "php tests/run.php",
        "test:js": "node tests/js/markdown.test.js",
        "test:phpunit": "phpunit --bootstrap api/config.php tests/"
    }
}
